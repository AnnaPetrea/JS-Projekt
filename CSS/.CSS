.css {
    background-color: #ede6d5;
    margin: 0;
    font-family: Arial, sans-serif;
    padding-left: 250px;
}

.side-menu-bar {
    position: fixed;
    /* Gør menuen fast i venstre side */
    top: 0;
    left: 0;
    height: 100%;
    /* Fylder hele højden af skærmen */
    width: 250px;
    /* Bredde på menuen */
    background-color: #333;
    color: white;
    display: flex;
    flex-direction: column;
    /* Stable elementer vertikalt */
    padding: 20px;
}

.burger-menu span {
    font-size: 30px;
    cursor: pointer;
}

.brand-name {
    font-size: 24px;
    font-weight: bold;
    text-align: center;
    margin: 20px 0;
}

.nav-buttons {
    display: flex;
    flex-direction: column;
    /* Vertikal opstilling af knapper */
    gap: 10px;
}

button {
    background-color: #ff7f50;
    color: white;
    border: none;
    padding: 10px;
    cursor: pointer;
    font-size: 16px;
    width: 100%;
    /* Knapperne fylder hele menuens bredde */
}

button:hover {
    background-color: #e67e22;
}